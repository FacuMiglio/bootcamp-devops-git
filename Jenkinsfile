pipeline {
    agent any
    stages {
        stage('Preparation') {
            steps {
                // Limpia el workspace antes de iniciar el build
                cleanWs()
                echo "Preparación del entorno"
            }
        }

        }
    }
